#include "omni_api.h"
#include <iostream>

void init_omni_engine() {
    std::cout << "OmniTrace Engine Initialized" << std::endl;
}

// WarpState is defined inline in the header (struct with default constructor)
// This file provides the engine initialization
